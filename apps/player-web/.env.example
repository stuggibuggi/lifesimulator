# API base URL baked into the static build
# Local:
VITE_API_URL=http://localhost:3001
# Plesk (same host if /api is proxied to Node, or full API URL):
# VITE_API_URL=https://vorsorgenavigator.stoffner.de
