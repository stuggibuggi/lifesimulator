# Optional override. Leave unset in production when /api is proxied on the same host.
# Local without Vite proxy:
# VITE_API_URL=http://localhost:3001
# Explicit production (usually unnecessary with same-origin proxy):
# VITE_API_URL=https://vorsorgenavigator.stoffner.de
