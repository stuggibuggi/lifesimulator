{
  "name": "goal-life-simulator",
  "version": "0.1.0",
  "private": true,
  "description": "GOAL – KI-gestütztes Lebenssimulationsspiel für Schüler",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/player-web",
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
